import { SearchMemberPipe } from './search-member.pipe';

describe('SearchMemberPipe', () => {
  xit('create an instance', () => {
    const pipe = new SearchMemberPipe();
    expect(pipe).toBeTruthy();
  });
});
